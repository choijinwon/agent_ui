# Assets

Place real image or icon files here when a generated spec references external
assets. By default, the agent uses inline SVG icon primitives and image
placeholder components, so no external files are required for the sample flow.
